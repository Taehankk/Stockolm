import BasicLayout from "../../layouts/BasicLayout";

const ReportWritePage = () => {
  return (
    <BasicLayout>
      <div>분석글작성폼입니다.</div>
    </BasicLayout>
  );
};

export default ReportWritePage;
